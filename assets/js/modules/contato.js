// Contato
window.mod.contato = function() {

	// Scope
	var that = this;

	var init = function() {
		console.log('[my] begin contato.js');
		window.mod['maps'] = new window.mod['maps']();
	};

	init();

};
